package com.Abstract;

/*	ABSTRACT
 * 	Def:-
 * 		An abstract class is a class which can contain the abstract methods
 * 		and is defined with the key word 'abstract'
 * 	
 * 	--> We cannot create an object for abstract Class because abstract class have abstract methods
 *  --> We can have non-abstract methods inside abstract class 
 *  --> we can implements multiple interfaces but we cannot extends multiple classes 
 *  
 */

public abstract class AbstractClass {

}
